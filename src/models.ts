export type JobHandlingAdditionnalArguments = {
  threadNumber: number;
  mapantApiWorkerId: string;
  mapantApiToken: string;
  mapantApiBaseUrl: string;
};

export type Extent = { minX: number; minY: number; maxX: number; maxY: number };
